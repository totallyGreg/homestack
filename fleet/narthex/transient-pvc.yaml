apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: transient-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
  storageClassName: ''
  volumeMode: Filesystem
  volumeName: transient-pv
status: {}
